import { environment } from "../../../../environments/environment";

// fall back configurations
export const Config = {
  isServed: false,
  apiUrl: environment.apiUrl,
  MyKey: 'default value',
  ExtraKeys: 'wont harm',
};
